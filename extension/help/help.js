/**
 * 帮助页面脚本
 */

// 初始化
function init() {
    console.log('帮助页面已加载');
    
    // 可以在这里添加一些交互逻辑
    // 例如：点击链接时的处理等
}

// 页面加载完成后初始化
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
} else {
    init();
}

