# 小红书反爬验证解决方案

## 🔍 问题分析

### 检测到的反爬特征

从您的截图可以看到：
```
Chrome 正受到自动测试软件的控制
```

这说明小红书检测到了以下特征：

### 1. **自动化检测特征**
```javascript
navigator.webdriver === true  // Playwright默认会设置这个
window.chrome === undefined   // 真实Chrome有这个对象
navigator.plugins.length === 0  // 真实浏览器有插件
```

### 2. **行为检测**
- 操作速度过快（毫秒级点击）
- 缺少鼠标移动轨迹
- 缺少滚动行为
- 页面停留时间过短

### 3. **会话检测**
- localStorage中的会话token缺失
- sessionStorage中的临时数据缺失
- Cookie中的特殊字段（如`sec_poison_id`、`websectiga`）

### 4. **浏览器指纹**
- Canvas指纹
- WebGL指纹
- 字体列表
- 音频指纹

## ✅ 综合解决方案

### 方案一：使用固定的浏览器Profile（推荐）

**原理**：第一次手动登录后，所有状态（Cookie、localStorage、指纹、会话）都保存在同一个`userDataDir`中，下次直接复用。

**优点**：
- ✅ 最简单有效
- ✅ 保持完整会话状态
- ✅ 无需额外依赖

**实施步骤**：

#### 1. 修改浏览器初始化配置

当前代码已经使用了`launchPersistentContext`，但每次可能创建新的profile。需要**固定使用同一个profile**：

```javascript
// 在 src/batch-processor.js 中
const scraper = new XiaohongshuScraper({
    // ... 其他配置
    reuseBrowserProfile: true,  // 已经设置了
    browserProfileId: 'xiaohongshu-login-profile'  // 固定使用这个profile
});
```

#### 2. 首次登录后保持Profile

**关键**：第一次登录后，`browser-data/xiaohongshu-login-profile` 目录会保存所有状态。

下次运行时，只要使用同一个profile，就不需要重新登录！

#### 3. 验证固定Profile是否生效

```bash
# 检查profile目录
ls -la browser-data/

# 应该看到固定的目录名
# xiaohongshu-login-profile/  ← 这个应该是固定的
```

### 方案二：隐藏自动化特征（进阶）

使用 `playwright-extra` 和 stealth插件隐藏自动化特征。

#### 1. 安装依赖

```bash
npm install playwright-extra puppeteer-extra-plugin-stealth
```

#### 2. 修改代码使用stealth模式

创建新文件 `src/stealth-browser.js`：

```javascript
/**
 * Stealth模式浏览器启动器
 * 隐藏Playwright的自动化特征
 */

const { chromium } = require('playwright-extra');
const StealthPlugin = require('puppeteer-extra-plugin-stealth');

// 添加stealth插件
chromium.use(StealthPlugin());

module.exports = chromium;
```

#### 3. 修改 `xiaohongshu-scraper.js`

```javascript
// 替换原来的 chromium 导入
// const { chromium } = require('playwright');
const chromium = require('./stealth-browser');

// 其他代码保持不变
```

### 方案三：模拟人类行为

在关键操作前后增加人类行为特征。

#### 1. 添加随机延迟

```javascript
// 在搜索前增加随机等待
async performSearch(keyword) {
    // 随机等待 2-5 秒
    const delay = 2000 + Math.random() * 3000;
    await this.page.waitForTimeout(delay);
    
    // 原有搜索逻辑...
}
```

#### 2. 添加鼠标移动

```javascript
// 模拟鼠标移动到搜索框
async moveMouseToSearchBox() {
    const searchBox = await this.page.$('input[autocomplete="off"]');
    if (searchBox) {
        const box = await searchBox.boundingBox();
        if (box) {
            // 随机移动到搜索框区域
            const x = box.x + box.width * Math.random();
            const y = box.y + box.height * Math.random();
            await this.page.mouse.move(x, y, { steps: 10 });
            await this.page.waitForTimeout(500 + Math.random() * 500);
        }
    }
}
```

#### 3. 添加页面滚动

```javascript
// 搜索结果加载后，模拟滚动浏览
async simulateScrolling() {
    await this.page.evaluate(async () => {
        // 随机滚动几次
        const scrollTimes = 3 + Math.floor(Math.random() * 3);
        for (let i = 0; i < scrollTimes; i++) {
            const scrollY = 300 + Math.random() * 500;
            window.scrollBy(0, scrollY);
            await new Promise(resolve => setTimeout(resolve, 1000 + Math.random() * 1000));
        }
    });
}
```

### 方案四：优化启动参数

修改Chromium启动参数，使其更接近真实浏览器。

```javascript
const context = await chromium.launchPersistentContext(userDataDir, {
    headless: false,
    args: [
        '--no-sandbox',
        '--disable-setuid-sandbox',
        '--disable-blink-features=AutomationControlled',  // 隐藏自动化特征
        '--disable-dev-shm-usage',
        '--disable-web-security=false',  // 改为false
        '--disable-features=IsolateOrigins,site-per-process',
        '--disable-features=VizDisplayCompositor',
        '--start-maximized',
        // 移除这些可能触发检测的参数
        // '--disable-gpu',
        // '--disable-accelerated-2d-canvas',
    ],
    // 添加更真实的选项
    viewport: { width: 1920, height: 1080 },
    locale: 'zh-CN',
    timezoneId: 'Asia/Shanghai',
    geolocation: { longitude: 116.407526, latitude: 39.904030 }, // 北京坐标
    permissions: ['geolocation']
});
```

## 🎯 推荐实施顺序

### 第1步：确保使用固定Profile（最重要）✅

```bash
# 1. 停止当前任务
npm run stop:web

# 2. 清理旧的profile目录
rm -rf browser-data/*

# 3. 检查配置（应该已经正确）
cat src/batch-processor.js | grep browserProfileId
# 应该看到：browserProfileId: 'shared-profile'

# 4. 重启服务
npm run start:web:background

# 5. 第一次：手动登录（会弹出扫码）
# 6. 第二次及以后：自动登录，无需扫码
```

### 第2步：添加Stealth模式（如果第1步不够）

```bash
npm install playwright-extra puppeteer-extra-plugin-stealth
```

然后应用方案二的代码修改。

### 第3步：添加行为模拟（最后）

如果前两步还不够，再添加方案三的人类行为模拟。

## 📊 效果对比

| 方案 | 难度 | 效果 | 需要修改 |
|------|------|------|----------|
| 固定Profile | ⭐ | ⭐⭐⭐⭐⭐ | 配置文件 |
| Stealth模式 | ⭐⭐ | ⭐⭐⭐⭐ | 少量代码 |
| 行为模拟 | ⭐⭐⭐ | ⭐⭐⭐ | 较多代码 |
| 优化参数 | ⭐ | ⭐⭐ | 配置文件 |

## 🔧 立即可用的快速修复

### 修改 `src/batch-processor.js`

```javascript
// 找到这一行（约177行）
cookieFile: './cookies.json'

// 确保上面有这两行配置
login: {
    method: 'manual',
    autoLogin: true,
    saveCookies: true,
    cookieFile: './cookies.json'
},
// 添加固定profile配置
reuseBrowserProfile: true,
browserProfileId: 'xiaohongshu-main',  // 改为更明确的名称
```

### 修改 `src/xiaohongshu-scraper.js` 启动参数

找到 `initBrowser()` 方法中的 `launchPersistentContext`，添加这个参数：

```javascript
const context = await chromium.launchPersistentContext(userDataDir, {
    headless: this.config.headless,
    args: [
        '--no-sandbox',
        '--disable-setuid-sandbox',
        '--disable-blink-features=AutomationControlled',  // 新增：隐藏自动化
        '--disable-dev-shm-usage',
        '--disable-accelerated-2d-canvas',
        '--no-first-run',
        '--no-zygote',
        '--start-maximized',
        '--disable-features=VizDisplayCompositor'
    ]
});
```

## 🚀 测试验证

```bash
# 1. 重启服务
npm run stop:web
npm run start:web:background

# 2. 第一次运行（需要扫码）
# 访问 http://localhost:3000
# 点击"开始下载"
# 在Chromium中扫码登录

# 3. 观察 browser-data 目录
ls -la browser-data/xiaohongshu-main/

# 应该看到：
# Default/
# ├── Cookies (SQLite数据库)
# ├── Local Storage/
# ├── Session Storage/
# └── 其他状态文件

# 4. 第二次运行（应该自动登录）
# 刷新页面，再次点击"开始下载"
# 应该直接进入搜索，不弹登录框
```

## 💡 关键点总结

1. **固定Profile最重要**：`browserProfileId: 'xiaohongshu-main'` 确保每次使用同一个目录
2. **首次完整登录**：第一次务必完成扫码，让所有状态保存
3. **不要删除browser-data**：这个目录包含所有登录状态
4. **复用浏览器进程**：如果可能，保持浏览器不关闭，连续运行多个任务

---

**立即操作**：现在最应该做的是确认 `browserProfileId` 是否固定使用同一个值！

